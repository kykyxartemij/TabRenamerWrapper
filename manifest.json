{
  "manifest_version": 3,
  "name": "Tab Wrapper Title",
  "version": "0.1.0",
  "description": "Open a wrapper page that sets a custom tab title and embeds the target URL in an iframe.",
  "permissions": ["tabs", "activeTab"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "Set tab title"
  }
}